/**
 * Interfaz utilizada para los combos o etiqueta HTML 'select'.
 */
export interface Select {
    /**
     * identificador del elemento seleccionado.
     */
    id: number | string;
    /**
     * nombre o texto del elemento seleccionado.
     */
    name: string;
}